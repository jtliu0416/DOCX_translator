# Must match the WebUI login service's deployment environment.
JWT_SECRET=
JWT_ISSUER=
JWT_AUDIENCE=

# Comma-separated browser origins allowed to call the API when cross-origin.
CORS_ALLOWED_ORIGINS=http://localhost:5173
